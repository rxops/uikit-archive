# NPM Configuration for RxOpsLibrary
# This file configures npm for the UIKit

# Registry configuration
registry=https://registry.npmjs.org/

# Package installation settings
legacy-peer-deps=true
fund=false
audit-level=moderate

# Cache and performance
prefer-offline=true
progress=true

# Security settings
package-lock=true

# Publishing configuration (for library publishing)
access=public
tag=latest

# Development settings
save-exact=false
save-prefix=^

# Optional: If using private registry or authentication
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Logging
loglevel=warn
